package org.uli.base;

import org.uli.util.Logger;
import org.uli.util.LoggerFactory;

public class Connection {
    final Logger logger = LoggerFactory.getLogger(Connection.class);

    public Connection() {
    }
}
